#!/usr/bin/env node
require('console.table');
const program = require('commander');
const { master, worker, agents } = require('./commands');

master(program);
worker(program);
agents(program);
program.parse(process.argv);




// const _DIR = process.cwd();

// let args = program.args;
